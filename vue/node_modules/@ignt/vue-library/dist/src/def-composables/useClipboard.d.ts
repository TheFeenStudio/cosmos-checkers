import type { ComputedRef } from "vue";
declare type ClipboardOptions = {
    resetAfter?: number;
};
export declare const useClipboard: (options?: ClipboardOptions) => {
    isSupported: boolean;
    copy: (value: string | ComputedRef<string>) => Promise<void>;
    hasCopied: import("vue").Ref<boolean>;
};
export {};
